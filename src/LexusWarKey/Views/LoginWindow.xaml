<Window x:Class="LexusWarKey.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Garena.mn WarKey — Нэвтрэх" Width="420" Height="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource B.Bg}"
        FontFamily="Segoe UI Variable Text, Segoe UI" UseLayoutRounding="True">
    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Image Source="/Assets/app.ico" Width="40" Height="40" VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality" Margin="0,0,12,0" />
            <TextBlock VerticalAlignment="Center" FontSize="22" FontWeight="Bold">
                <Run Text="Lexus" Foreground="{StaticResource B.Blue}" /><Run Text=" WarKey" Foreground="{StaticResource B.Text}" />
            </TextBlock>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,18,0,0" TextAlignment="Center" TextWrapping="Wrap"
                   Foreground="{StaticResource B.Dim}" FontSize="13"
                   Text="WarKey-г ашиглахын тулд Discord акаунтаараа нэвтэрнэ үү." />

        <TextBlock Grid.Row="2" x:Name="StatusText" Margin="0,20,0,0" TextAlignment="Center"
                   VerticalAlignment="Center" TextWrapping="Wrap"
                   Foreground="{StaticResource B.Dim}" FontSize="12" Text="" />

        <Button Grid.Row="3" x:Name="LoginButton" Height="46" Click="Login_Click"
                Cursor="Hand" BorderThickness="0" Foreground="White" FontSize="15" FontWeight="SemiBold">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#5865F2" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#6B75F5" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#2A2D33" />
                            <Setter Property="Foreground" Value="{StaticResource B.Dim}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            Discord-оор нэвтрэх
        </Button>

        <Button Grid.Row="4" x:Name="CancelButton" Margin="0,10,0,0" Height="34" Click="Cancel_Click"
                Cursor="Hand" Background="Transparent" BorderThickness="0"
                Foreground="{StaticResource B.Dim}" FontSize="12" Content="Гарах" />
    </Grid>
</Window>
